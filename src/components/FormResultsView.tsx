// Shim de compatibilidade: o conteúdo de FormResultsView foi refatorado e
// dividido em módulos menores dentro de ./forms/FormResults/.
// Este arquivo mantém os imports antigos (`from './FormResultsView'`) funcionando.
export * from './forms/FormResults/FormResultsView';
